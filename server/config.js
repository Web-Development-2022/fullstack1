const config = {
    db: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'todo'
    }
}

module.exports = config;    // we can import this module whenever we likely
